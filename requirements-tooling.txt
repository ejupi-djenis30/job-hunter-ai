# Desktop packaging/test tools, deliberately kept out of application environments.
# Regenerate requirements-tooling.lock with:
# pip-compile --generate-hashes --allow-unsafe --output-file requirements-tooling.lock requirements-tooling.txt
pip-tools==7.6.1
pyinstaller==6.22.2
# PyInstaller declares macholib only when resolving on macOS. Keep it explicit so this
# single hash-locked file remains installable on every release runner.
macholib==1.16.4
pytest==9.1.1
pytest-asyncio==1.4.0
