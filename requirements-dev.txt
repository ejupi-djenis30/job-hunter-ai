# Complete development environment. Regenerate requirements-dev.lock with:
# pip-compile --generate-hashes --allow-unsafe --output-file requirements-dev.lock requirements-dev.txt

-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-mock==3.15.1
httpx2==2.10.0

# Pinned editable-install backend. Use pip --no-build-isolation so this reviewed
# version is used without fetching an implicit build environment.
setuptools==83.0.0
wheel==0.48.0

ruff==0.16.3
mypy==2.3.1
types-reportlab==4.5.1.20260728
pre-commit==4.6.2

pip-audit==2.10.1
pip-licenses==5.5.5
