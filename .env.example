# ═══════════════════════════════════════
# Job Hunter AI — Environment Variables
# ═══════════════════════════════════════
# Copy this file to backend/.env and fill in your values.
#   cp .env.example backend/.env

# ─── JWT Authentication ───
# REQUIRED: Set a strong random secret for JWT token signing
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET_KEY=CHANGE_ME_TO_A_RANDOM_SECRET

# ─── LLM Provider Configuration ───
# Provider: "groq" or "deepseek"
LLM_PROVIDER=deepseek

# ─── API Keys (set the one for your chosen provider) ───
GROQ_API_KEY=your-groq-api-key-here
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# ─── Model ───
# Groq models: moonshotai/kimi-k2-instruct-0905, llama-3.3-70b-versatile, etc.
# DeepSeek models: deepseek-reasoner (R1, best reasoning), deepseek-chat (V3, fast)
LLM_MODEL=deepseek-reasoner

# ─── Generation Parameters ───
LLM_MAX_TOKENS=64000
LLM_TEMPERATURE=0.7
LLM_TOP_P=1.0
LLM_FREQUENCY_PENALTY=0.0
LLM_PRESENCE_PENALTY=0.0

# ─── DeepSeek Thinking Mode ───
# Set to "true" to enable Chain-of-Thought reasoning (deepseek-reasoner only)
LLM_THINKING=true

# ─── Advanced: Override base URL (optional) ───
# LLM_BASE_URL=https://api.groq.com/openai/v1

# ─── Database (optional) ───
# DATABASE_URL=sqlite:///./job_hunter.db
