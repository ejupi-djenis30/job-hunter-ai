# ═══════════════════════════════════════
# Job Hunter AI — Environment Variables
# ═══════════════════════════════════════
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ─── Authentication ───
# Required: generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32
JWT_SECRET_KEY=your-secret-key-here

# Auth provider: "simple" (built-in JWT) or "supabase" (hosted auth + database)
AUTH_PROVIDER=simple

# ─── Supabase (required if AUTH_PROVIDER=supabase) ───
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_KEY=your-anon-key
# SUPABASE_SERVICE_KEY=your-service-role-key

# Provider: groq, deepseek, or gemini
LLM_PROVIDER=gemini

# ─── API Keys (set the one for your chosen provider) ───
# For Groq:     GROQ_API_KEY=your-groq-api-key
# For DeepSeek: DEEPSEEK_API_KEY=your-deepseek-api-key
# For Gemini:   GEMINI_API_KEY=your-gemini-api-key
# Alternatively, use the unified key:
LLM_API_KEY=your-api-key-here

# ─── Model Override ───
# Groq:     moonshotai/kimi-k2-instruct-0905, llama-3.3-70b-versatile
# DeepSeek: deepseek-reasoner (R1 reasoning model), deepseek-chat (V3.2 fast chat)
# Gemini:   gemini-2.0-flash-thinking-exp-01-21, gemini-1.5-flash
# LLM_MODEL=gemini-1.5-flash

# ─── DeepSeek Thinking Mode ───
# "true" = Chain-of-Thought reasoning (deepseek-reasoner only)
# LLM_THINKING=false

# ─── Gemini Thinking Level ───
# OFF, LOW, MEDIUM, HIGH (only applies to Gemini-2.0-flash-thinking models)
# LLM_THINKING_LEVEL=MEDIUM

# ─── Advanced LLM Parameters ───
# LLM_MAX_TOKENS=16384
# LLM_TEMPERATURE=0.7
# LLM_TOP_P=1.0
# LLM_BASE_URL=https://api.groq.com/openai/v1

# ─── Database ───
# Default: SQLite for local dev. Set to PostgreSQL for production.
# DATABASE_URL=sqlite:///./job_hunter.db
# DATABASE_URL=postgresql://user:pass@localhost:5432/jobhunter

# ─── Server ───
# API_HOST=127.0.0.1
# API_PORT=8000

# ─── CORS ───
# Comma-separated origins (default: http://localhost:5173,http://localhost:8000)
# CORS_ORIGINS=http://localhost:5173,http://localhost:8000

# ─── Logging ───
# LOG_LEVEL=INFO

# ─── Docker / Postgres (only for docker-compose) ───
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_USE_A_STRONG_PASSWORD
POSTGRES_DB=jobhunter
